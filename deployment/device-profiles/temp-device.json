[
{
    "apiVersion": "v3",
    "device": {
      "name": "Sensor1",
      "description": "A test sensor device",
      "adminState": "UNLOCKED",
      "operatingState": "UP",
      "protocols": {
        "mqtt": {
          "host": "172.100.150.5", 
          "port": "1883",
          "topic": "xcx/data",
          "CommandTopic": "test"
        }
      },
      "labels": ["MQTT", "sensor"],
      "profileName": "Generic-MQTT-String-Float-Device",
      "serviceName": "device-mqtt"
    }
  }
]

kubectl delete cm asa-location && cluster_name=asa && helm install ai-model ../AI-helm \
--set deployment.name=$cluster_name-ai \
--set container.name=$cluster_name-ai \
--set configmapName=$cluster_name-python \
--set env[0].name=CLUSTER_NAME \
--set env[0].value=$cluster_name \